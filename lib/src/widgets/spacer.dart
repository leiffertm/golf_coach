import 'package:flutter/widgets.dart';
class VSpacer extends SizedBox { const VSpacer(double h, {super.key}) : super(height: h); }
class HSpacer extends SizedBox { const HSpacer(double w, {super.key}) : super(width: w); }
